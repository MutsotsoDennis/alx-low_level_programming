#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
FILE *passwordFile = fopen("password.txt", "w");
if (passwordFile == NULL)
{
perror("Error opening file");
return 1;
}
fprintf(passwordFile, "your_password_here");
fclose(passwordFile);

return 0;
}
